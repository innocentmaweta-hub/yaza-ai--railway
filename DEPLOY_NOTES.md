This repository contains only deployment wrapper files (Dockerfile / Procfile / .dockerignore) that clone and build the upstream YAZA-DeepVision repository during image build.

I intentionally did not copy or modify application source files so the app's behaviour and appearance remain exactly as in the original repository (https://github.com/Ajay-paka/YAZA-DeepVision).

If you'd like me to instead copy the entire application into this repository (so it is self-contained), I can do that, but it will increase the repo size and I will need to copy all files from the upstream project.
